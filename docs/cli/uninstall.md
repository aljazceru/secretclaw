---
summary: "CLI reference for `secretclaw uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `secretclaw uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
secretclaw uninstall
secretclaw uninstall --all --yes
secretclaw uninstall --dry-run
```
